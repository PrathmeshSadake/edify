import { Nav } from "@/components/nav";
import ToolsGrid from "@/components/tools-grid";

const HomePage = () => {
  return (
    <div>
      <ToolsGrid />
    </div>
  );
};

export default HomePage;
